1. git init

2. git status

3. git add . or git add filename

4. git commit -m "message"

5. git remote add origin "rep url"

6. git pull origin master

(create a test file and push to github. Let the file name be test.txt)

7. git status (to check if there is any un commited code)  --if there is uncommited code, the file name will be in red otherwise should be green

8. git add test.txt

(now if we check status it will say commited)

9. git commit -m "message" (git commit -a -m "message" in case of multiple files) 

10. git push origin master


(update the file)

(repeat steps 4 -7)

git remote set-url origin https://github.com/ypraghavendra/Test2-Sep-13.git

----
----


$ git config --global user.name "King Kong"
$ git config --global user.email "king-kong@gmail.com"

******************